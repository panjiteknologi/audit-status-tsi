import { AllProject } from '@/types/Project';

interface ChartInfoProps {
  data: AllProject[];
}

const ChartInfo = ({ data }: ChartInfoProps) => {
  return (
    <div>ChartInfo</div>
  )
}

export default ChartInfo